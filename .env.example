# ── Airtable ───────────────────────────────────────────────────────────────────
# Genera tu token en: https://airtable.com/create/tokens
# Permisos necesarios: data.records:read  data.records:write
AIRTABLE_API_KEY=pat_xxxxxxxxxxxxxxxxxx

# ── SMTP (tu hosting, Ionos, SiteGround, etc.) ────────────────────────────────
SMTP_HOST=mail.tudominio.com
SMTP_PORT=587
SMTP_USER=info@aceitedirecto.com
SMTP_PASSWORD=tu_contraseña_aqui

# ── Email donde recibes el resumen diario ─────────────────────────────────────
# Si no lo pones, llega al mismo SMTP_USER
RESUMEN_EMAIL=santiago@tudominio.com
